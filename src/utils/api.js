
// An instance for general backend stuff
const instance = {};

// An instance scoped ONLY to the API endpoints. Use this 99% of the time
const apiInstance = {};

// Convenience functions
const get = (url, data = {}) =>
{
};

const post = (url, data = {}, config = {}) =>
{
};

const put = (url, data = {}, config = {}) =>
{
};

const patch = (url, data = {}, config = {}) =>
{
};

const destroy = (url, data = {}, config = {}) =>
{
};

const cancellable = (type, url, data = {}) =>
{
};

export { instance, apiInstance, get, post, put, patch, destroy, cancellable };
